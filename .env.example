# Server-only GitHub GraphQL credential. Never expose this value to the browser.
GITHUB_TOKEN=

# Server-only secret for POST /api/revalidate/github.
# Generate a long random value and send it in the x-revalidate-secret header.
REVALIDATE_SECRET=
